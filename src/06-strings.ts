(() => {
  let productTitle = 'my amazing product';
  // productTitle = null;

  const productDescripcion = 'iadsa aff';

  let productPrice = 100;
  let isNew = true;
  const summary = `
    producttitle: ${productTitle}
    descripcion: ${productDescripcion}
    price: ${productPrice}
    isNew: ${isNew}

  `;
  console.log(summary);

  let myName: string = '';
})();
